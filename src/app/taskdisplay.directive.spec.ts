import { TaskdisplayDirective } from './taskdisplay.directive';

describe('TaskdisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskdisplayDirective();
    expect(directive).toBeTruthy();
  });
});
